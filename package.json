{
  "name": "shadowbyte-web",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'WASM package prebuilt and ready'",
    "build:wasm": "wasm-pack build --target web --out-dir www/pkg",
    "dev": "npx serve www"
  }
}
